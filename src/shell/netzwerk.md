# [Netzwerk-Konfiguration](https://wiki.debian.org/NetworkConfiguration)

**`ip`**, `ifconfig`, `route`, _`nmtui`_

**`ping`**, `traceroute`, `tracepath`, **`dig`**

`netstat`, _`ss`_

`hostname`


`/etc/network/interfaces`

`/etc/resolv.conf`

`/etc/hosts`

`/etc/hostname`

---
